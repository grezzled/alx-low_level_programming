#include "main.h"
/**
 * _isdigit - A function that returns if it's digit
 * @c: character to check
 * Return:  1 | 0
 */
int _isdigit(int c)
{
if (c >= '0' && c <= '9')
{
return (1);
}
else
{
return (0);
}
}
