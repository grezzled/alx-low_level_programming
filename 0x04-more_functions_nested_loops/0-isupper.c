#include "main.h"
/**
 * _isupper - A function that returns the upper case string
 * @c: character to check
 * Return:  1 | 0
 */
int _isupper(int c)
{
if (c >= 'A' && c <= 'Z')
{
return (1);
}
else
{
return (0);
}
}
